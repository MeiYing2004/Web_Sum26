// DEPRECATED — accounts consolidated into auth-service `users` table (bus_auth).
// Saved passengers: auth-service `saved_passengers` + GetSavedPassengers gRPC.
// This file is kept only for historical reference; user-service no longer uses Prisma.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
