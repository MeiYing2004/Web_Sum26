generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model SavedPassenger {
  id       String @id @default(cuid())
  userId   String
  fullName String
  phone    String
  email    String
  idNumber String?
}
